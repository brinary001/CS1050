#include <stdio.h>
#include <stdlib.h>

int main(void) 

{
	printf("Enter a positive number: ");
	
	int num1;
	int num2 = 0;
	
	scanf("%d", &num1);
	while (num1 <= num2) {
		puts("Number should be positive enter the number again");
	
	while {
		(num1 > num2) {
		puts("The positive number entered is %d", num1);
	}	
	}
	if (num1 == -1) {
		printf("Exiting program");
		exit(0);
	}	

	}
}